import style from '../Pricing/Pricing.module.css'

const Pricing = () =>{
    return(
        <div className={`${style.pricing} container text-center`}>
            <h1>Hello! At the moment this is test version. Check later</h1>
        </div>
    )
}

export default Pricing