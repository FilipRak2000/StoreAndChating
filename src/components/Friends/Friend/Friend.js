const Friend = (props) =>{
    return(
        <div>
            <h1>{props.name}</h1>
            <button>check files</button>
            <button>chat</button>
        </div>
    )
}

export default Friend